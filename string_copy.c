#include<stdio.h>
#include<string.h>
	int main()
	{
		char str1[20] = "Sakshi More ";
		char str2[20];

		strcpy(str2,str1);

		puts(str2);                  //used to write a line or string to the output( stdout ) stream

		return 0;

	}

